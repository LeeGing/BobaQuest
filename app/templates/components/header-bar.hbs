{{yield}}

<div data-sticky-container>
  <div data-sticky data-margin-top='0' data-top-anchor="header:bottom" data-btm-anchor="content:bottom">
    <div class="top-bar topbar-sticky-shrink">
      <div class="top-bar-title">
        <nav class="top-bar topbar-responsive">
          <div class="top-bar-title">
            <a class="topbar-responsive-logo" href="#"></a>
          </div>
          <div id="topbar-responsive" class="topbar-responsive-links">
            <div class="top-bar-left">
              {{#basic-dropdown as |dropdown|}}
                {{#dropdown.trigger }}
                  <button class="loginBtn loginBtn--google">Menu</button>
                  {{/dropdown.trigger}}
                {{#dropdown.content class="content-bootstrap-feel width-300 slide-fade"}}
                  {{#if session.isAuthenticated}}
                    <ul>
                      <li class="loginBtn loginBtn--google">
                        <button {{action 'processLogout'}}>
                          Logout
                        </button>
                      </li>
                      {{#link-to 'users' userHash.id}}
                        <li class="loginBtn loginBtn--google">
                          <button>User Profile</button>
                        </li>
                      {{/link-to}}
                      {{#link-to 'welcome'}}
                        <li class="loginBtn loginBtn--google">
                          <button>Home page</button>
                        </li>
                      {{/link-to}}
                      {{#link-to 'stores' 1}}
                        <li class="loginBtn loginBtn--google">
                          <button>Store Page</button>
                        </li>
                      {{/link-to}}
                    </ul>
                  {{else}}
                    <ul>
                      {{#link-to 'login' }}
                        <li class="loginBtn loginBtn--google">
                          <button>Login</button>
                        </li>
                      {{/link-to}}
                      {{#link-to 'register'}}
                        <li class="loginBtn loginBtn--google">
                          <button>Registration</button>
                        </li>
                      {{/link-to}}
                      {{#link-to 'welcome' }}
                        <li class="loginBtn loginBtn--google">
                        <button>Home page</button>
                        </li>
                      {{/link-to}}
                      <li class="loginBtn loginBtn--facebook">
                        <button {{action 'processLogin'}} >
                         Login with Facebook
                        </button>
                      </li>
                      <li class="loginBtn loginBtn--facebook">
                        <button {{action 'leaveFacebook'}} >
                         Logout
                        </button>
                      </li>
                     </ul>
                  {{/if}}
                {{/dropdown.content}}
              {{/basic-dropdown}}
              {{log userHash}}
              {{#if userHash.username}}
                <h6 style="font-family: 'Quicksand', sans-serif; float: left; display: block;">Logged in as: {{userHash.username}}</h6>
              {{/if}}
            </div>
          </div>
          <h3 class="title1">BoBa Quest</h3>
        </nav>
      </div>
    </div>
  </div>
</div>